"""Reaction layout engine (see §6 of the specification)."""

from __future__ import annotations

from typing import Any


def render_reaction(spec: dict[str, Any]) -> str:
    """Render a reaction scheme JSON spec to a single SVG string."""
    raise NotImplementedError("TODO(M2): implement reaction layout engine")
