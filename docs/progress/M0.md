# M0 — Repository initialization & identity hygiene

## Status

Completed 2026-08-13, except for the maintainer-only checklist below and the
CI green-light confirmation (recorded in a follow-up commit).

## Identity

- NetBerth uses the alias `random-orbit <305297779+random-orbit@users.noreply.github.com>`.
- The global git identity on this machine was a *different* alias
  (`onetwothree <251001341+onetwothree-star@users.noreply.github.com>`).
- Per the spec ("git identity must match NetBerth's alias exactly"), this repo
  overrides identity locally:
  - `user.name = random-orbit`
  - `user.email = 305297779+random-orbit@users.noreply.github.com`
- Maintainer reminder: keep the global identity aligned or always rely on the
  repo-local override. `scripts/check_pii.sh` and CI re-verify this on commit.

## Repository

- `gh` CLI is authenticated as `random-orbit` (GitHub user id `305297779`).
- Created standalone public repo under the NetBerth alias account (transferred
  to the chemglyph organization on 2026-08-13):
  <https://github.com/chemglyph/chemglyph>
  - License: MIT
  - Description: "Publication-quality chemical structure & reaction rendering for AI agents"
- Local `origin` points at the new repo; no NetBerth code or history was imported.

## Scaffold

- `pyproject.toml` (hatchling, src layout, `chemglyph-mcp` console script,
  `rdkit>=2024.9` + `mcp>=1.2`, `opsin`/`dev` extras)
- `LICENSE` (MIT), `.gitignore` (Python template + `.DS_Store`;
  `answer_key.json` intentionally NOT ignored)
- `src/chemglyph/` module skeleton with public API stubs (`errors.py` added for
  the required `ChemGlyphError` hierarchy)
- `tests/`, `benchmarks/`, `examples/`, `docs/progress/`, `docs/launch/`
- CI: GitHub Actions, ruff + pytest on ubuntu-latest and macos-latest,
  Python 3.11/3.13, plus a commit-author alias check

## PII protection

- `scripts/check_pii.sh` scans staged files and configured identity against
  `scripts/pii_patterns.txt` (gitignored, placeholder written locally).
- `scripts/install_hooks.sh` installs the pre-commit hook.
- CI verifies the push commit author is exactly the alias identity; it does not
  depend on the local patterns file.

## Self-checks

- `git log --format='%an %ae'` shows only `random-orbit
  <305297779+random-orbit@users.noreply.github.com>`.
- `gh api user` returns login `random-orbit` (id `305297779`).
- CI first run: GREEN on all four jobs (ubuntu + macos x Python 3.11/3.13),
  including the commit-author alias check.

## Identity incident handled during push

- `gh repo create --license mit` auto-generated a remote initial commit whose
  author email was the GitHub account's then-public mailbox address (a
  third-party provider, not a noreply alias; the literal address has been
  redacted from this repository). That single redundant LICENSE commit was
  replaced with `--force-with-lease` so the visible branch history contains
  only the noreply identity. See REVIEW-1.md for the residual-exposure audit
  and the dangling-commit purge recommendation.
- Maintainer action: set the GitHub account's public email to
  `305297779+random-orbit@users.noreply.github.com` (or make it private) so
  future server-side commits do not leak a personal address.

## Maintainer checklist (manual)

- [ ] Fill `scripts/pii_patterns.txt` with real sensitive patterns (gitignored).
- [ ] Confirm <https://github.com/chemglyph/chemglyph> is visible and CI is green.
- [ ] GitHub account Settings -> Emails: check "Keep my email addresses private".
- [ ] GitHub account Settings -> Emails: check "Block command line pushes that
      expose my email".
- [ ] Set the GitHub account public email to the noreply alias (see incident above).
- [ ] Register the `chemglyph` name on PyPI (optionally publish a `0.0.1`
      placeholder package before the real `0.1.0` release).
- [ ] Confirm no real name/email appears in repo contents or commit history.
